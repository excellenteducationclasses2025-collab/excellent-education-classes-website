{
  "frontend/src/App.tsx": "Main application component with TanStack Router setup including five routes (home, admin-login, admin, live-classes, register) — adds the new `/register` route pointing to `RegistrationPage`.",
  "frontend/src/components/Layout.tsx": "Layout component with Holi festival themed header navigation; adds a visually distinct \"Register\" button linking to `/register` alongside the existing Home, Live Classes, and Admin Login navigation items.",
  "frontend/src/pages/HomePage.tsx": "Homepage with Holi festival theme; the hero CTA buttons now navigate to live-classes and admin-login respectively, with all references to student-login removed throughout the page.",
  "frontend/src/pages/RegistrationPage.tsx": "Full student registration page with six fields (Name, Mobile Number, Email, Gender, School Name, Class), client-side validation, Holi festival theme styling with decorative elements, and a success confirmation screen — no authentication required.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin dashboard for managing student registrations with search, sort, approve/reject/delete actions, and stats cards; updated to use `reg.schoolName` and `reg.mobileNumber` instead of the removed `reg.collegeOrSchool` field, and added a Mobile column to the registrations table.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interaction; updated `useSubmitRegistration` to pass all six fields (including `mobileNumber` and `schoolName`), fixed `useGetRegistrationsByFilter` to use `schoolName`/`mobileNumber` instead of the old `collegeOrSchool`, and updated `useUpdateRegistration` to match the new backend signature.",
  "frontend/src/index.css": "Global styles with OKLCH color system customized for Holi festival theme using vibrant magenta, pink, yellow, blue, green, and orange colors with custom animations for color splashes, powder bursts, and floating gulal effects.",
  "frontend/src/pages/StudentLogin.tsx": "Student login page using Internet Identity authentication; the \"Don't have an account? Register here\" link has been removed entirely.",
  "frontend/src/pages/StudentProfile.tsx": "Student profile page with Holi festival theme featuring integrated manual UPI payment system, study materials section displaying both Physics and Contemporary India PDFs with payment-gated access, Holi color decorations, and color splash animation upon payment confirmation.",
  "frontend/src/pages/AdminLogin.tsx": "Admin login page that validates email against the authorized admin email, triggers Internet Identity authentication, and stores the email in sessionStorage before redirecting to the admin dashboard.",
  "frontend/package.json": "Package configuration file with added canvas-confetti dependency for payment success animations.",
  "frontend/tailwind.config.js": "Tailwind configuration extended with Holi festival theme colors (magenta, pink, yellow, blue, green, orange), custom animations (color-splash, powder-burst, color-float, splash-wave), and Holi-themed shadow effects.",
  "frontend/src/pages/LiveClasses.tsx": "New Live Class Portal page displaying 6 hardcoded live class sessions in a responsive grid, each card showing subject badge, title, description, instructor, scheduled date/time, and a colored \"Join Live Class\" button opening the session URL in a new tab, styled with Holi theme colors, animations, and decorative gulal/splash elements.",
  "frontend/src/hooks/useActor.ts": "Updated actor hook that waits for AuthClient initialization (`isInitializing === false`) before enabling the query, exposes an `isReady` flag that is only `true` when initialization is complete AND the actor query has successfully resolved with a non-null actor, and includes `isInitializing` in the `isFetching` composite state.",
  "frontend/src/hooks/useActorReady.ts": "New wrapper hook around `useActor` that adds an `isReady` boolean flag — true only when AuthClient initialization is complete (`!isInitializing`), the actor is not fetching, and the actor is non-null. Exposes `{ actor, isFetching, isReady }`."
}